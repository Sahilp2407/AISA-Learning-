rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Open for development and academic demo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
